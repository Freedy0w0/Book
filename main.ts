let 我的變數 = randint(1, 6)
basic.showNumber(我的變數)
if (我的變數 == 6) {
    basic.showIcon(IconNames.Happy)
} else if (我的變數 == 3) {
    basic.showIcon(IconNames.Asleep)
} else {
    basic.showIcon(IconNames.Sad)
}
basic.forever(function () {
	
})
